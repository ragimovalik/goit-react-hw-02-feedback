import Button from '../Button/Button';

const Buttons = () => {
  return (
    <>
      <Button btnText={'Good'} />
      <Button btnText={'Neutral'} />
      <Button btnText={'Bad'} />
    </>
  );
};

export default Buttons;
